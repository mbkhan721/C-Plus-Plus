#include<iostream>
using namespace std;

int nlhw() {
	cout << endl;
	/*
	// 1.
	cout << "\n1.\n";

	int n;
	cout << "Enter an odd integer between 10 and 20: ";
	cin >> n;

	while (n <= 10 || n >= 20 || n % 2 == 0) {
		cout << "Enter an odd integer between 10 and 20: ";
		cin >> n;
	}

	for (int r = 1; r <= n; r++) {
		for (int c = 1; c <= n; c++) {
			if (c == r || c == n - r + 1 || r == 1 || c == 1 || r == n || c == n ) {
				cout << "x ";
			}
			else
				cout << "- ";
		}
		cout << endl;
	}

	
	// 2.
	cout << "\n2.\n";

	int x;
	cout << "Enter an integer: ";
	cin >> x;

	for (int i = 1; i <= x; i++) {
		if (x % i == 0) {
			for (int j = 1; j <= i; j++) {
				cout << "x ";
			}			
		}
		cout << endl;
	}
	*/


	cout << endl;
	return 0;
}