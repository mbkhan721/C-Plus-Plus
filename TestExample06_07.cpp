#include<iostream>
using namespace std;

int tesEx() {
	/*
	// 2.
	double money = 5.35;
	cout << (money  - static_cast<int>(money)) << endl << endl;
	
	// Expressions 1.
	float a = 20, b = 6;

	// int c = static_cast<int>(a) / static_cast<int>(b);
	// float c = a / b;
	int c = static_cast<int>(a) % static_cast<int>(b);

	cout << c << endl << endl;

	
	// 2.
	srand(time(NULL));
	cout << rand() % 13 - 8 << endl << endl;

	
	// Branching 1.
	int year;
	cout << "Enter a year: ";
	cin >> year;
	if (year % 100 == 0) {
		cout << "Century year" << endl;
	}
	else {
		cout << "Not a century year" << endl << endl;
	}
	
	// 2.
	int grade;
	cout << "Enter a grade: ";
	cin >> grade;
	if (grade <= 100 && grade > 89) {
		cout << "A" << endl;
	}
	else if (grade < 90 && grade > 79) {
		cout << "B" << endl;
	}
	else if (grade < 80 && grade > 69) {
		cout << "C" << endl;
	}
	else {
		cout << "F" << endl << endl;
	}
	
	int x, y;
	cout << "Enter two int: ";
	cin >> x;
	cin >> y;
	if (y % x == 0) {
		cout << "X Y is multiple" << endl;
	}
	
	int x, y;
	cout << "Enter two integers: ";
	cin >> x >> y;
	if (x % y == 0) {

		cout << "X Multiple" << endl;

	}

	if (y % x == 0) {

		cout << "Y Multiple" << endl;

	}
	*/



	return 0;
}