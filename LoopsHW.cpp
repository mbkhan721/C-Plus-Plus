#include<iostream>
using namespace std;

int loopsHW() {
	cout << endl;
	/*
	// 1.
	int n, digit;
	int sum = 0;
	do {
		cout << "Enter an integer between 1 and 100,000 inclusive: ";
		cin >> n;
	} while (n < 1 || n > 100000);

	int backup = n;
	for (int i = 0; i <= n; ++i) {
		digit = n % 10;
		n /= 10;
		sum += digit;
	}
	cout << "The sum of all digits in " << backup << " is " << sum << endl;
	

	// 2.
	int n;
	int counter = 0;
	cout << "Enter an integer: ";
	cin >> n;

	for (int i = 2; i < n; ++i) {
		if (n % i == 0) {
			counter++;
			cout << i << " is a factor of " << n << endl;
		}		
	}
	if (counter == 0) {
		cout << n << " is prime." << endl;
	}
	*/

	cout << endl;
	return 0;
}