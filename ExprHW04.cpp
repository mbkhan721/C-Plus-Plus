#include<iostream>
using namespace std;

int expHw() {
	/*
	// 1.
	int x, digit;
	cout << "Enter a 3-digit integer: ";
	cin >> x;
	int y = x;

	digit = x % 10;

	cout << "Is " << digit << " a factor of " << y << "? " << (y % digit == 0) << endl;
	x /= 10;
	digit = x % 10;
	cout << "Is " << digit << " a factor of " << y << "? " << (y % digit == 0) << endl;
	x /= 10;
	digit = x % 10;
	cout << "Is " << digit << " a factor of " << y << "? " << (y % digit == 0) << endl << endl;
	
	

	int dollars;
	cout << "Enter a dollar amount as an integer: ";
	cin >> dollars;
	cout << dollars << endl;
	dollars %= 25;

	cout << "Optimal number of dimes: " << (dollars / 10) << endl;
	*/


	return 0;
}